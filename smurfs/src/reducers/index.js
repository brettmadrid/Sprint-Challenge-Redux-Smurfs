const initialState = {
  smurfs: [],
  fetchingSmurfs: false,
  addingSmurf: false,
  error: null
};

const Reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default Reducer;

